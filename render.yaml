services:
  - type: web
    name: cryptobot
    env: node
    buildCommand: npm install && npm run db:generate && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
